# 安全策略

PoXiao 是安全扫描工具，请务必**仅对已获授权的目标**使用。任何未经授权的扫描都可能违反法律，使用者须自行承担全部责任。

## 支持的版本

| 版本 | 受支持 |
|------|--------|
| 3.0.x | ✅ |

## 报告安全漏洞

请**不要**通过公开 Issue 报告安全漏洞。请通过以下任一渠道发送：

- 在 GitHub 上创建 **Private vulnerability report**（仓库 → Security → Report a vulnerability）。
- 或发送至项目维护者的安全邮箱（见仓库主页联系方式）。

请在你的报告中包含：

- 复现步骤与受影响版本。
- 详细的漏洞描述。
- 可选的修复建议。

我们会尽快确认并回复。在漏洞被修复前，请勿公开披露。

## 安全最佳实践

- 默认配置绑定 `127.0.0.1`，面向公网部署时请务必开启认证并修改默认口令。
- 生产环境扫描建议将 `scan.verify_ssl` 置为 `true`，避免中间人篡改情报响应。
- 切勿将 API 密钥（NVD / Shodan / FOFA / Censys / GitHub Token）写入仓库或配置文件后提交。