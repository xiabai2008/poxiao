# AGENTS.md — 破晓 (PoXiao)

## Agent skills

### Issue tracker

Issues live in GitHub Issues at https://github.com/xiabai2004/poxiao. Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default canonical label names: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` at the repo root, one `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.
