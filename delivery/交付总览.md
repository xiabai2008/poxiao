# 破晓（PoXiao）v3.0.0 · AICoding 架构方案 · 交付总览（G6）

> 由 AICoding 架构专家团（主理人 + 5 核心架构师 + 2 增强角色）基于项目背景与资料全量产出。
> 全链路 Gate：G0→G1→G2→G3→G4→G5→G6 全部通过。**按用户新规：所有 Gate 自动校验通过即放行，不弹窗、不向用户提问；中间确认由主理人直接裁决。**

---

## 一、交付物清单（已归档 `delivery/`）

| 文档 | Owner | Gate | 自动校验 | 落盘路径 |
| --- | --- | --- | --- | --- |
| 资料摘要 material_digest.md | 闻资料 | G1 | 7/7 | `delivery/material_digest.md` |
| 行业调研报告 research_report.md | 查有据 | G2 | 12/12 | `delivery/research_report.md` |
| 高层架构设计 | 许边界 | G3 | 12/12 | `delivery/高层架构设计.md` |
| 系统设计 | 高见远 | G4 | 11/11 | `delivery/系统设计.md` |
| UserStory | 顾全景 | G4 | 5/5 | `delivery/UserStory.md` |
| 部署设计 | 毕落地 | G5 | 8/8 | `delivery/部署设计.md` |
| 安全设计 | 严守正 | G5 | 9/9 | `delivery/安全设计.md` |
| 部署拓扑图 | 毕落地 | G5 | — | `delivery/部署拓扑.drawio` |

---

## 二、已冻结裁决（全程唯一事实源，下游不得推翻）

| 编号 | 裁决 | 落点 |
| --- | --- | --- |
| X1 | POC 模板以代码实际 **215** 个为准；内置 CVE 以代码实际 **257** 条为准（README 的 206/121 为陈旧，README 已修正） | 高层架构 §4.4 / 系统设计 / 安全设计 §4.1 / 部署设计 §4.5 门禁 |
| X2 | WAF 绕过 = 可选模块，默认关闭，不在 MVP 主链路 | 高层架构 Out-of-Scope O2 / 系统§4.1 / 部署§3.2.1 / 安全§5.2.1 |
| X3 | 存储 = GuanXing 监控 SQLite 单文件（WAL）+ 报告 JSON/Markdown 文件；**禁用 Postgres/Redis/Docker** | 高层架构 §4.4 / 系统§4.4 / 部署§1 / 安全§3.1 |
| Q4 | CVE 内置 257（系统架构核验，README 121 陈旧，已修正） | 系统设计 |
| Q5 | 报告引擎 = Python 标准库（json + 字符串模板），**不引入 Jinja2** | 系统设计 / 安全§4.1 |
| Q7 | 模块 taxonomy 映射（info_leak→sensitive 等）已锁定 | 系统设计 M1~M6 |
| 云基线 | need_cloud_baseline_check = false（跳过 CloudQ 云核对） | 全程 |

---

## 三、MVP 与 Out-of-Scope（一致性已对齐）

- **MVP**：F1~F13（破晓 Dawn + 霜月/春分/惊蛰/观星/夏至 5 工具）+ N1~N2，全部 MVP✅
- **Out-of-Scope**：O1 GuanXing Web 界面（auth 默认关）/ O2 WAF 绕过 / O3 SaaS / O4 HTML 报告
- **GuanXing 鉴权**：`monitor.auth` 默认 **false**（仅绑 127.0.0.1 回环不构成未授权访问）；生产启用路径已固化（开 auth → 改密 → Token → 可选 MFA）

---

## 四、重叠区权威分配（G5 骨架交接结论）

| 重叠项 | 权威方 | 另一侧引用 | 一致性 |
| --- | --- | --- | --- |
| 网络拓扑骨架（VPC/子网/CIDR） | 毕落地 | 严守正按拓扑落实 | 本地单机，无 VPC ✅ |
| 安全组/WAF/DDoS | 严守正 | 毕落地按规则配置 | WAF 默认关、禁 0.0.0.0 ✅ |
| KMS/Vault 选型 | 毕落地 | 严守正定义分级/轮转 | 本地 AES-256 + keychain ✅ |
| 密钥分级/轮转 | 严守正 | 毕落地按此配置 | 5 类一致 ✅ |
| 日志管道/存储 | 毕落地 | 严守正定义字段/保留期 | 结构化 JSON ✅ |
| 审计字段/保留期 | 严守正 | 毕落地按此配置 | ≥ 1 年 ✅ |

---

## 五、G5 交叉 diff 六项对账（通过）

VPC（无）/ 安全组（禁 0.0.0.0+出站白名单）/ KMS（AES-256+keychain）/ 密钥分级（5类引用一致）/ 日志保留期（≥1年，部署≥安全）/ WAF（可选默认关）——**全部一致，无冲突项**。

---

## 六、下游待办（已转交，主理人记录）

1. **README 修正**：模板数 206→**215**，CVE 121→**257**（X1/Q4，已完成）。
2. **CONTEXT.md / ADR 补建**：已完成（X1/X2/X3/Q5/MVP 冻结决议固化于仓库根 `CONTEXT.md`）。
3. **CVE 数据治理**：原 `CVE-2022-21661` 重复实为虚构 PHP mbstring 记录，已删除并补入真实 `CVE-2021-21708`（见 `CONTEXT.md` §4）。
4. **GuanXing auth 默认 false**：安全设计已固化启用路径，待用户生产落地。
5. **verify_ssl 默认 false**：安全设计建议生产置 true（已声明待用户决策，非阻塞）。

---

## 七、结论

破晓 v3.0.0 架构方案五份主文档（资料/调研/高层/系统/用户故事/部署/安全）全量产出、逐级 Gate 通过、术语与冻结裁决统一、G5 重叠区一致。**G6 可归档交付。**

— 主理人（AICoding 架构专家团）归档于 `delivery/`，2026-07-09
